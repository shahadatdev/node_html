const firstSide = 10;
const secondSide = 20;
const thirdSide = 5;

console.log (`the triangle peremeter is ${firstSide+secondSide+thirdSide}
side length are ${firstSide}, ${secondSide}, ${thirdSide}`)