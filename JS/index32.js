const hight = 10;
const base = 4;
const perimeter = 2 * (hight + base);
const area = hight * base;

console.log(`the rectangle peremeter is ${perimeter}`);
console.log(`the rectanglr area is ${area}`);
console.log(`the hight lenth is ${hight} and the base length is ${base}`);

